# Headless Chart Service Environment Variables

# Supabase Configuration (Required for Phase 2 multi-worker coordination)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_SCHEMA=headless

# Worker Configuration (Optional)
WORKER_ID=worker-1                    # Unique worker ID (auto-generated if not set)
WORKER_MAX_JOBS=3                      # Max concurrent jobs this worker can handle

# Service Configuration
PORT=3100                              # HTTP server port
MAX_CONCURRENT_JOBS=3                  # Max concurrent browser contexts
MAX_CONTEXTS=5                         # Maximum browser contexts to maintain
BROWSER_IDLE_TIMEOUT=300000           # Browser idle timeout in ms (5 minutes)
HEALTH_TIMEOUT_MS=15000               # Health check timeout in ms

# Application URLs
FRONTEND_URL=http://localhost:5174    # Frontend application URL
BACKEND_URL=http://localhost:8000     # Backend API URL

# Logging
LOG_LEVEL=info                        # Log level: debug, info, warn, error