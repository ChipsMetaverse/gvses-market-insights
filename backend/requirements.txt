fastapi==0.110.0
uvicorn[standard]>=0.31.1
httpx>=0.27.1
slowapi>=0.1.4
pydantic>=2.6.0,<3.0.0
supabase>=2.18.0
python-multipart>=0.0.9
websockets==13.1  # Pin to exact version with additional_headers support
SpeechRecognition==3.10.0
gTTS==2.5.0
numpy==1.26.0
python-dotenv==1.0.0
joblib>=1.3.0  # Required for pattern_confidence_service
# pyaudio==0.2.14  # Requires PortAudio, install separately if needed
anthropic==0.25.0
beautifulsoup4==4.12.3
pytz==2024.1
openai==1.105.0
playwright>=1.45.0
tenacity>=8.2.3  # Retry logic with exponential backoff
prometheus-client>=0.20.0  # Sprint 1 Day 2: Prometheus metrics

# PDF Processing (Keep minimal for now, review later)
pypdf==3.17.4

# Dataset builder dependencies
PyYAML==6.0.1
jsonschema==4.22.0

# Testing Dependencies (Keep for now)
pytest>=7.0.0,<8.0.0         # Test framework
pytest-asyncio>=0.21.0       # Async test support