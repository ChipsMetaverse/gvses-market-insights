/**
 * Sentry Configuration - Minimal stub
 * Sentry error tracking is disabled by default
 */

export function initSentry() {
  // Sentry disabled - no DSN configured
  // To enable: Set VITE_SENTRY_DSN environment variable
  return;
}
